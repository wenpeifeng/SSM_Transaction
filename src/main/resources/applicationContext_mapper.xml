<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context
       http://www.springframework.org/schema/context/spring-context.xsd">

    <!-- 读取属性文件jdbc.properties -->
    <context:property-placeholder location="jdbc.properties"/>

    <!-- 创建数据源 -->
    <bean id="dataSource" class="com.alibaba.druid.pool.DruidDataSource">
        <!-- 以下value值指向的是jdbc.properties里设置的属性 -->
        <property name="driverClassName" value="${jdbc.driverClassName}"/>
        <property name="url" value="${jdbc.url}"/>
        <property name="username" value="${jdbc.username}"/>
        <property name="password" value="${jdbc.password}"/>
    </bean>

    <!-- 配置SqlSessionFactoryBean类 -->
    <bean class="org.mybatis.spring.SqlSessionFactoryBean">
        <!-- 配置数据源 (ref指向的就是上面<bean>中的id值，druid的数据源) -->
        <property name="dataSource" ref="dataSource"/>
        <!-- 配置MyBatis的核心配置文件（value值是我们创建的核心配置文件） -->
        <property name="configLocation" value="SqlMapConfig.xml"/>
        <!-- 注册实体类的别名（value值是我们存放实体类所在的包名）-->
        <property name="typeAliasesPackage" value="com.ahead.pojo"/>
    </bean>

    <!-- 注册mapper.xml文件 -->
    <bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
        <!-- value值是我们存放mapper文件所在的包名 -->
        <property name="basePackage" value="com.ahead.dao"/>
    </bean>

</beans>