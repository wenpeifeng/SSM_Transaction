<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!-- 下方注释掉的这些，在applicationContext_mapper.xml文件都有配置，不能配置两套，否则会冲突-->
    <!-- 读取属性文件中数据库的配置 -->
    <!--    <properties resource="db.properties"/>-->

    <settings>
        <!-- 设置日志输出语句，显示相应操作的sql语名 -->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
        <!--开启驼峰命名规则自动转换-->
        <setting name="mapUnderscoreToCamelCase" value="true" />
    </settings>
    <!-- 注册实体类的别名 -->
    <!--   <typeAliases>
           <package name="com.ahead.pojo"/>
       </typeAliases>-->
    <!-- 数据源 -->
    <!--    <environments default="development">
            <environment id="development">
                <transactionManager type="JDBC"></transactionManager>
                <dataSource type="POOLED">
                    <property name="driver" value="com.mysql.jdbc.Driver"/>
                    <property name="url" value="jdbc:mysql://localhost:3306/ssm?useSSL=false&amp;
                    serverTimezone-URC&amp;allowPublicKeyRetrieval=true"/>
                    <property name="username" value="root"/>
                    <property name="password" value="123456"/>
                </dataSource>
            </environment>
        </environments>-->
    <!-- 注册mapper.xml文件 -->
    <!--    <mappers>
            <package name="mapper文件所在的包名"/>
        </mappers>-->
</configuration>